Implemented KAN-2 foundations.

Changed:
- Added Shopping profile storage in `src/shopping/profile-store.mjs`
- Added queued append-only audit log in `src/shopping/audit-log.mjs`
- Added standalone Settings / Shopping pane in `src/shopping/settings.html`
- Added browser settings renderer in `src/shopping/settings-pane.mjs`
- Added `package.json` with `npm test`
- Added tests in `test/shopping.test.mjs`
- Updated `spec/KAN-2/plan.md`
- Wrote Jira summary to `spec/KAN-2/response.md`

Validation: `npm test` passes.

Note: there is no existing app shell/router in this repo, so the Settings pane is a standalone static entry point rather than integrated into a larger UI.